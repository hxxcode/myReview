mabatis使用时手动编写映射文件时会比较麻烦，mybatis generator工具可以帮我们自动生成Mapper.xml映射文件以及dao，model等文件。
常用的有两种方式：
一.命令行方式
1.工具准备：
   需要下载mybatis-generator-core-1.3.2.jar，MySQL-connector-Java-5.1.34.jar（对应相关数据库的驱动包），generator.xml（配置文件）。
2.配置generator.xml文件：指定一下我们的dao，do，xml，doexample的位置

3.生成文件：
打开命令行，进入generator目录，执行如下命令：
  java -jar mybatis-generator-core-1.3.2.jar -configfile generator.xml -overwrite
  
  注意生成的文件我们需要在xml文件中修改一下类名，改成dao。并对dao类加注解respoisty
  
然后进入到相应目录就可以看到生成的文件了，之后将文件内容复制到我们的项目中。
二.maven插件方式：
1.工具准备：
在mybatis官网下载Generator工具。
2.在pom.xml中添加plugin：
打开下载的工程文件后，找到pom.xml文件，在文件中添加如下代码：


其中generatorConfig.xml文件的位置我们可以按照需求自己指定。
3.配置generatorConfig.xml文件：
基本和上面类似

4.生成文件：
使用mvn clean package命令，在我们之前设置的相应位置生成DoMapper.xml，Domapper.java,Do,Doexample文件。

如果不需要获取插入数据的id（主键）值，不需要加generatedKey，如果加了generatedKey 对于id不是自增主键的情况，id值不在insert语句里进行插入。
selectKey会在插入之前或者之后（order指定）来获取插入数据的id
